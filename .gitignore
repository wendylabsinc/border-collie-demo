__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
.venv314/
.venv-training/
dist/
build/
artifacts/runs/
artifacts/fruit-model/
lab/*/model/
lab/*/artifacts/
runs/
.DS_Store

# Stagefile build output is regenerated from the committed source and lock.
Dockerfile.generated
Dockerfile.generated.dockerignore

# The two demo model binaries are deliberately versioned: they are part of the
# reproducible demo checkpoint and the branch must stay deployable as-is.
# Everything else under artifacts/, lab/*/model/, and runs/ stays ignored.
!media/model/model.engine
!media/model/banana-specialist.pt
